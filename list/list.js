/**
 * Generates list content
 * 
 * @param {Object} data - An array contents multiple articles
 * @returns {String} Returns a list text
 */
function list(data) {
    return '<ul>article body</ul>';
}
